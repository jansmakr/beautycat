# 📦 전국 피부관리실 데이터 업로드 가이드

**파일**: upload-public-data.js  
**버전**: v1.0  
**작성일**: 2025-12-30  
**예상 시간**: 50분 (30,000개 기준)

---

## 🎯 **개요**

이 스크립트는 `전국피부미용실 현황251231.csv` 파일을 읽어서 BeautyCat 데이터베이스에 자동으로 업로드합니다.

### **주요 기능**
- ✅ CSV 파일 자동 파싱 (EUC-KR 인코딩 지원)
- ✅ 주소 자동 분리 (시/도, 구/군, 동/읍/면)
- ✅ 영업중 데이터만 필터링
- ✅ 중복 자동 제거
- ✅ 배치 업로드 (1,000개씩)
- ✅ 실시간 진행률 표시
- ✅ 자동 재시도 (실패 시)
- ✅ 결과 리포트 생성

---

## 📋 **사전 준비**

### **1. 파일 확인**

다음 파일들이 같은 폴더에 있어야 합니다:

```
D:\beautycat\
  ├── upload-public-data.js          (업로드 스크립트)
  └── 전국피부미용실 현황251231.csv   (데이터 파일)
```

### **2. Node.js 설치 확인**

```cmd
node --version
```

**출력 예시:** `v18.17.0` 또는 그 이상

만약 설치되지 않았다면:
- https://nodejs.org 에서 다운로드
- LTS 버전 설치 (18.x 이상)

### **3. iconv-lite 설치 (한글 인코딩)**

```cmd
npm install iconv-lite
```

**⚠️ 중요**: 이 패키지가 없으면 한글이 깨질 수 있습니다!

---

## 🚀 **실행 방법**

### **방법 1: 간단 실행** (권장 ✅)

```cmd
cd /d D:\beautycat

node upload-public-data.js
```

### **방법 2: 단계별 실행**

#### **Step 1: 폴더 이동**
```cmd
cd /d D:\beautycat
```

#### **Step 2: CSV 파일 확인**
```cmd
dir "전국피부미용실 현황251231.csv"
```

**출력 예시:**
```
2025-12-31  오전 09:00        67,123,456 전국피부미용실 현황251231.csv
```

#### **Step 3: 스크립트 실행**
```cmd
node upload-public-data.js
```

#### **Step 4: 확인 후 엔터**
```
⚠️  30000개 데이터를 업로드하시겠습니까?
   예상 시간: 약 50분

   계속하려면 엔터를 누르세요 (취소: Ctrl+C)
```

**→ 엔터 누르기**

---

## 📊 **실행 화면 예시**

### **초기 화면**
```
═══════════════════════════════════════════════
  전국 피부관리실 데이터 업로드
  버전: v1.0
  날짜: 2025-12-30
═══════════════════════════════════════════════

📂 CSV 파일 읽는 중: 전국피부미용실 현황251231.csv
📋 컬럼: 업체명, 업체주소, 인허가상태명, ...
✅ 총 50000개 레코드 읽기 완료

🧹 데이터 정제 시작...
  ✓ 영업중 필터링: 50000개 → 35000개
  ✓ 필수 필드 확인: 35000개 → 32000개
  ✓ 중복 제거: 32000개 → 30000개
✅ 정제 완료: 최종 30000개
```

### **업로드 진행 중**
```
🚀 업로드 시작: 총 30000개
📦 배치 크기: 1000개
📊 총 30개 배치

📤 배치 1/30 업로드 중... (1000개)
  ✅ 배치 1 완료: 성공 1000, 실패 0
📈 진행률: 3.3% | 경과: 1.5분 | 예상: 45.0분

📤 배치 2/30 업로드 중... (1000개)
  ✅ 배치 2 완료: 성공 1000, 실패 0
📈 진행률: 6.7% | 경과: 3.0분 | 예상: 45.0분

...

📤 배치 30/30 업로드 중... (1000개)
  ✅ 배치 30 완료: 성공 1000, 실패 0
📈 진행률: 100.0% | 경과: 48.5분 | 예상: 48.5분
```

### **완료 화면**
```
🎉 업로드 완료!
✅ 성공: 30000개
❌ 실패: 0개
⏱️  소요 시간: 48.5분

📄 결과 저장: upload-result.json
```

---

## 📁 **결과 파일**

업로드 완료 후 `upload-result.json` 파일이 생성됩니다:

```json
{
  "timestamp": "2025-12-30T12:34:56.789Z",
  "total": 30000,
  "success": 30000,
  "fail": 0,
  "successRate": "100.00%"
}
```

---

## ⚠️ **문제 해결**

### **문제 1: "CSV 파일을 찾을 수 없습니다"**

**원인:** 파일 경로가 잘못됨

**해결:**
```cmd
# 1. 현재 폴더 확인
cd

# 2. 파일 목록 확인
dir

# 3. CSV 파일이 있는 폴더로 이동
cd /d D:\beautycat
```

---

### **문제 2: "한글이 깨져요"**

**원인:** iconv-lite 패키지 미설치

**해결:**
```cmd
npm install iconv-lite
```

---

### **문제 3: "HTTP 500 오류"**

**원인:** API 서버 오류 또는 네트워크 문제

**해결:**
1. 인터넷 연결 확인
2. 몇 분 후 재시도
3. 실패한 데이터는 자동으로 재시도됨 (최대 3회)

---

### **문제 4: "중간에 멈췄어요"**

**원인:** 네트워크 끊김 또는 시스템 오류

**해결:**
1. **Ctrl+C**로 중단
2. 이미 업로드된 데이터는 유지됨
3. 다시 실행하면 **중복 제거** 기능이 작동하여 안전

---

### **문제 5: "속도가 너무 느려요"**

**원인:** API 제한 또는 네트워크 속도

**해결:**
```javascript
// upload-public-data.js 수정
const CONFIG = {
  BATCH_SIZE: 500,    // 1000 → 500으로 줄이기
  DELAY_MS: 50,       // 100 → 50으로 줄이기 (주의!)
};
```

**⚠️ 주의:** DELAY_MS를 너무 줄이면 API 제한에 걸릴 수 있습니다!

---

## 🔍 **데이터 검증**

### **업로드 후 확인 방법**

#### **방법 1: 브라우저 (추천 ✅)**

```
1. https://beautycat.kr/admin-dashboard.html 접속
2. 관리자 로그인
3. "공공데이터 관리" 메뉴
4. 총 개수 확인: 30,000개
```

#### **방법 2: API 직접 호출**

```javascript
// 브라우저 콘솔 (F12)에서 실행
fetch('/tables/public_skincare_data?limit=1')
  .then(res => res.json())
  .then(data => {
    console.log('총 개수:', data.total);
    console.log('샘플 데이터:', data.data[0]);
  });
```

**예상 결과:**
```json
{
  "total": 30000,
  "data": [
    {
      "id": "abc123...",
      "business_name": "강남 뷰티살롱",
      "address": "서울특별시 강남구 역삼동 123-45",
      "region": "서울",
      "district": "강남구",
      "town": "역삼동",
      "status": "영업중"
    }
  ]
}
```

---

## 📊 **예상 소요 시간**

| 데이터 개수 | 배치 수 | 예상 시간 | 실제 시간 (평균) |
|------------|---------|----------|-----------------|
| 10,000개 | 10개 | 17분 | 15-20분 |
| 20,000개 | 20개 | 33분 | 30-40분 |
| **30,000개** | **30개** | **50분** | **45-55분** |
| 40,000개 | 40개 | 67분 | 60-75분 |

**변수:**
- 인터넷 속도
- API 서버 부하
- 시스템 성능

---

## 🔧 **고급 설정**

### **CONFIG 변수 설명**

```javascript
const CONFIG = {
  CSV_FILE: '전국피부미용실 현황251231.csv',  // CSV 파일 경로
  API_BASE_URL: '/tables',                    // API URL
  TABLE_NAME: 'public_skincare_data',         // 테이블 이름
  BATCH_SIZE: 1000,                           // 배치 크기 (조정 가능)
  DELAY_MS: 100,                              // 요청 간 대기 (ms)
  MAX_RETRIES: 3,                             // 재시도 횟수
};
```

### **성능 튜닝**

#### **빠르게 하려면:**
```javascript
BATCH_SIZE: 2000,   // 배치 크기 증가
DELAY_MS: 50,       // 대기 시간 감소
```

#### **안정적으로 하려면:**
```javascript
BATCH_SIZE: 500,    // 배치 크기 감소
DELAY_MS: 200,      // 대기 시간 증가
MAX_RETRIES: 5,     // 재시도 증가
```

---

## 📋 **체크리스트**

### **실행 전**
- [ ] Node.js 설치 확인 (`node --version`)
- [ ] CSV 파일 위치 확인
- [ ] iconv-lite 설치 (`npm install iconv-lite`)
- [ ] 인터넷 연결 확인

### **실행 중**
- [ ] 진행률 모니터링
- [ ] 에러 메시지 확인
- [ ] 예상 시간 확인

### **실행 후**
- [ ] upload-result.json 확인
- [ ] 성공률 확인 (100% 목표)
- [ ] 관리자 대시보드에서 데이터 확인
- [ ] 샘플 데이터 검증 (5개 이상)

---

## 🎉 **완료 후 다음 단계**

### **1. 데이터 검증** (5분)
```
✓ 총 개수 확인: 30,000개
✓ 지역 분포 확인: 서울, 경기, 부산...
✓ 샘플 데이터 확인: 주소 파싱 정확도
```

### **2. 지역별 페이지 생성** (2시간)
```
generate-region-pages.js 실행
→ 3,500개 동별 페이지 생성
→ /region/seoul-gangnam-yeoksam.html
```

### **3. 샵 개별 페이지 생성** (2시간)
```
generate-shop-pages.js 실행
→ 30,000개 샵 페이지 생성
→ /shop/gangnam-beauty-salon-abc123.html
```

### **4. 사이트맵 업데이트** (10분)
```
generate-sitemap.js 실행
→ 33,000+ URL로 확장
→ 네이버/구글에 재제출
```

### **5. 성과 모니터링** (지속)
```
주간: 색인 현황 확인
월간: 트래픽 리포트
분기: SEO 전략 검토
```

---

## 📞 **지원**

문제가 발생하면:

1. **에러 메시지 확인**
2. **upload-result.json 확인**
3. **이 문서의 "문제 해결" 섹션 참조**
4. **관리자에게 문의**

---

## 📝 **변경 이력**

| 버전 | 날짜 | 변경사항 |
|------|------|----------|
| v1.0 | 2025-12-30 | 초기 버전 |

---

**문서 버전**: v1.0  
**최종 수정**: 2025-12-30  
**작성자**: AI Assistant  
**문의**: BeautyCat 관리자
