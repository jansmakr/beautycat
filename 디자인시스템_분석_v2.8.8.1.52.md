# 🎨 BeautyCat.kr 디자인 시스템 분석 v2.8.8.1.52

## 📅 작업 정보
- **버전**: v2.8.8.1.52
- **날짜**: 2026-01-18
- **분석 이미지**: 5개 (홈/디자인 시스템/지역/혜택/내정보)
- **작업자**: BeautyCat Dev Team

---

## 🎯 디자인 시스템 분석 결과

### 📊 분석 이미지 목록
1. **Image 1 (AJ61P3N4)**: 홈 화면 (추천 피부관리실)
2. **Image 2 (fjxRmfDw)**: 디자인 시스템 시트 (컬러/타이포그래피/컴포넌트)
3. **Image 3 (kPqSNtLf)**: 지역 탭 (지도 + 필터)
4. **Image 4 (Pzf8fiDI)**: 혜택 탭 (쿠폰/포인트)
5. **Image 5 (Pjuy9V7L)**: 내정보 탭 (프로필/설정)

---

## 🎨 1. 컬러 팔레트 (Color Palette)

### Primary Colors
```css
/* 라벤더 계열 (Primary Lavender) */
--primary-lavender: #E0D7E6;      /* 메인 컬러 */
--primary-lavender-dark: #C8B8D6; /* 호버/액티브 */
--primary-lavender-light: #F0EBF4; /* 배경 */

/* 블러쉬 계열 (Primary Blush) */
--primary-blush: #F3E6E6;         /* 강조 컬러 */
--primary-blush-dark: #E8CFCF;    /* 호버/액티브 */
--primary-blush-light: #FAF3F3;   /* 배경 */
```

### Secondary Colors
```css
/* 파스텔 퍼플 */
--secondary-purple: #D0C7E8;      /* 버튼, 칩 */
--secondary-purple-dark: #B8A8D6; /* 호버 */

/* 파스텔 핑크 */
--secondary-pink: #F8D7DC;        /* 배지, 알림 */
--secondary-pink-dark: #F0BFC7;   /* 호버 */
```

### Background Colors
```css
--bg-primary: #FFFFFF;            /* 메인 배경 */
--bg-secondary: #F9F7FB;          /* 섹션 배경 */
--bg-tertiary: #F3F0F6;           /* 카드 배경 */
--bg-overlay: rgba(0, 0, 0, 0.05); /* 오버레이 */
```

### Text Colors
```css
--text-primary: #2D2D2D;          /* 제목, 본문 */
--text-secondary: #6B6B6B;        /* 보조 텍스트 */
--text-muted: #A0A0A0;            /* 플레이스홀더, 힌트 */
--text-link: #8B7BB8;             /* 링크 */
--text-white: #FFFFFF;            /* 버튼 텍스트 */
```

### Accent Colors
```css
--accent-success: #B8E0B8;        /* 성공 메시지 */
--accent-warning: #FFE5B4;        /* 경고 메시지 */
--accent-error: #FFD4D4;          /* 에러 메시지 */
--accent-info: #D4E5FF;           /* 정보 메시지 */
```

### Gradient (for buttons, banners)
```css
/* 라벤더 → 블러쉬 그라데이션 */
background: linear-gradient(135deg, #E0D7E6 0%, #F3E6E6 100%);

/* 파스텔 퍼플 → 라이트 그라데이션 */
background: linear-gradient(180deg, #D0C7E8 0%, #E8DCF5 100%);
```

---

## 📝 2. 타이포그래피 (Typography)

### Font Family
```css
font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
```

### Heading Styles
```css
/* H1 - 페이지 제목 */
.heading-1 {
  font-size: 28px;       /* 1.75rem */
  font-weight: 700;      /* Bold */
  line-height: 1.3;
  letter-spacing: -0.02em;
  color: var(--text-primary);
}

/* H2 - 섹션 제목 */
.heading-2 {
  font-size: 22px;       /* 1.375rem */
  font-weight: 600;      /* Semibold */
  line-height: 1.4;
  letter-spacing: -0.01em;
  color: var(--text-primary);
}

/* H3 - 카드 제목 */
.heading-3 {
  font-size: 18px;       /* 1.125rem */
  font-weight: 600;      /* Semibold */
  line-height: 1.5;
  color: var(--text-primary);
}

/* H4 - 서브 제목 */
.heading-4 {
  font-size: 16px;       /* 1rem */
  font-weight: 500;      /* Medium */
  line-height: 1.5;
  color: var(--text-secondary);
}
```

### Body Styles
```css
/* Body Large */
.body-large {
  font-size: 16px;       /* 1rem */
  font-weight: 400;      /* Regular */
  line-height: 1.6;
  color: var(--text-primary);
}

/* Body Regular */
.body-regular {
  font-size: 14px;       /* 0.875rem */
  font-weight: 400;      /* Regular */
  line-height: 1.6;
  color: var(--text-primary);
}

/* Body Small */
.body-small {
  font-size: 12px;       /* 0.75rem */
  font-weight: 400;      /* Regular */
  line-height: 1.5;
  color: var(--text-secondary);
}
```

### Caption & Label
```css
/* Caption */
.caption {
  font-size: 11px;       /* 0.6875rem */
  font-weight: 400;      /* Regular */
  line-height: 1.4;
  color: var(--text-muted);
}

/* Label */
.label {
  font-size: 13px;       /* 0.8125rem */
  font-weight: 500;      /* Medium */
  line-height: 1.4;
  color: var(--text-primary);
}
```

---

## 📏 3. 레이아웃 & 간격 (Layout & Spacing)

### Container
```css
/* 모바일 우선 (Mobile-First) */
.container {
  max-width: 100%;
  padding-left: 16px;
  padding-right: 16px;
  margin: 0 auto;
}

/* 태블릿 */
@media (min-width: 768px) {
  .container {
    max-width: 720px;
    padding-left: 24px;
    padding-right: 24px;
  }
}

/* 데스크탑 */
@media (min-width: 1024px) {
  .container {
    max-width: 960px;
    padding-left: 32px;
    padding-right: 32px;
  }
}
```

### Spacing Scale (Tailwind 기반)
```css
--spacing-0: 0px;
--spacing-1: 4px;
--spacing-2: 8px;
--spacing-3: 12px;
--spacing-4: 16px;
--spacing-5: 20px;
--spacing-6: 24px;
--spacing-8: 32px;
--spacing-10: 40px;
--spacing-12: 48px;
--spacing-16: 64px;
```

### Card Spacing
```css
.card {
  padding: 16px;          /* 내부 여백 */
  margin-bottom: 16px;    /* 카드 간격 */
  gap: 12px;              /* 내부 요소 간격 */
}

@media (min-width: 768px) {
  .card {
    padding: 20px;
    margin-bottom: 20px;
  }
}
```

### Section Gaps
```css
.section {
  padding-top: 32px;
  padding-bottom: 32px;
}

@media (min-width: 768px) {
  .section {
    padding-top: 48px;
    padding-bottom: 48px;
  }
}
```

---

## 🧩 4. 컴포넌트 (Components)

### Button Styles

#### Primary Button (라벤더 배경)
```css
.btn-primary {
  background: linear-gradient(135deg, #E0D7E6 0%, #F3E6E6 100%);
  color: #2D2D2D;
  padding: 12px 24px;
  border-radius: 20px;      /* 매우 둥근 모서리 */
  font-size: 15px;
  font-weight: 600;
  border: none;
  box-shadow: 0 2px 8px rgba(224, 215, 230, 0.4);
  transition: all 0.2s ease;
}

.btn-primary:hover {
  background: linear-gradient(135deg, #C8B8D6 0%, #E8CFCF 100%);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(224, 215, 230, 0.5);
}

.btn-primary:active {
  transform: translateY(0);
  box-shadow: 0 2px 4px rgba(224, 215, 230, 0.3);
}
```

#### Secondary Button (아웃라인)
```css
.btn-secondary {
  background: transparent;
  color: #8B7BB8;
  padding: 12px 24px;
  border-radius: 20px;
  font-size: 15px;
  font-weight: 600;
  border: 2px solid #E0D7E6;
  transition: all 0.2s ease;
}

.btn-secondary:hover {
  background: #F0EBF4;
  border-color: #C8B8D6;
}
```

#### Small Button (칩/태그)
```css
.btn-small {
  background: #F3F0F6;
  color: #6B6B6B;
  padding: 6px 14px;
  border-radius: 16px;      /* 둥근 모서리 */
  font-size: 13px;
  font-weight: 500;
  border: none;
  transition: all 0.2s ease;
}

.btn-small:hover {
  background: #E0D7E6;
  color: #2D2D2D;
}

.btn-small.active {
  background: #D0C7E8;
  color: #FFFFFF;
}
```

### Card Styles
```css
.card {
  background: #FFFFFF;
  border-radius: 20px;      /* 둥근 모서리 */
  padding: 16px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06); /* Soft Elevation */
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1); /* Card Shadow */
}
```

### Input Field Styles
```css
.input-field {
  background: #F9F7FB;
  border: 1px solid #E0D7E6;
  border-radius: 16px;      /* 둥근 모서리 */
  padding: 12px 16px;
  font-size: 14px;
  color: #2D2D2D;
  transition: all 0.2s ease;
}

.input-field:focus {
  outline: none;
  border-color: #C8B8D6;
  background: #FFFFFF;
  box-shadow: 0 0 0 3px rgba(224, 215, 230, 0.2);
}

.input-field::placeholder {
  color: #A0A0A0;
}
```

### Badge/Chip Styles
```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  background: #F8D7DC;      /* 파스텔 핑크 */
  color: #2D2D2D;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 500;
}

.badge-success {
  background: #B8E0B8;
}

.badge-warning {
  background: #FFE5B4;
}

.badge-info {
  background: #D4E5FF;
}
```

### Bottom Navigation Bar
```css
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #FFFFFF;
  border-top: 1px solid #F3F0F6;
  padding: 8px 16px;
  display: flex;
  justify-content: space-around;
  align-items: center;
  box-shadow: 0 -2px 12px rgba(0, 0, 0, 0.06);
  z-index: 1000;
}

.bottom-nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  color: #A0A0A0;
  font-size: 11px;
  font-weight: 500;
  transition: all 0.2s ease;
}

.bottom-nav-item.active {
  color: #8B7BB8;
}

.bottom-nav-item:hover {
  color: #6B5A9E;
}
```

### Floating Action Button (FAB)
```css
.fab {
  position: fixed;
  bottom: 80px;             /* 네비게이션 바 위 */
  right: 16px;
  width: 56px;
  height: 56px;
  background: linear-gradient(135deg, #E0D7E6 0%, #F3E6E6 100%);
  color: #2D2D2D;
  border-radius: 50%;       /* 원형 */
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 16px rgba(224, 215, 230, 0.5);
  transition: all 0.3s ease;
  z-index: 999;
}

.fab:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 20px rgba(224, 215, 230, 0.6);
}

.fab:active {
  transform: scale(0.95);
}
```

---

## 🎭 5. 비주얼 스타일 (Visual Style)

### Overall Aesthetic
- **스타일**: Modern, Minimalist, Soft & Playful
- **분위기**: 따뜻하고 친근한, 프리미엄 느낌
- **타겟**: 20-40대 여성, 펫 케어에 관심 있는 사용자

### Border Radius Philosophy
```css
/* 매우 둥근 모서리 (Very Rounded) */
--border-radius-small: 12px;    /* 칩, 배지 */
--border-radius-medium: 16px;   /* 입력 필드, 작은 카드 */
--border-radius-large: 20px;    /* 버튼, 큰 카드 */
--border-radius-xl: 24px;       /* 모달, 섹션 */
--border-radius-full: 50%;      /* 원형 버튼, 프로필 */
```

### Shadow Values
```css
/* Soft Elevation (기본 카드) */
box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);

/* Card Shadow (호버 시) */
box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);

/* Button Shadow */
box-shadow: 0 2px 8px rgba(224, 215, 230, 0.4);

/* FAB Shadow */
box-shadow: 0 4px 16px rgba(224, 215, 230, 0.5);
```

### Icon Style
- **타입**: Outlined (아웃라인) 스타일
- **라이브러리**: Font Awesome 6.4.0 (현재 사용 중)
- **크기**: 18px (small), 20px (medium), 24px (large)
- **컬러**: `--text-secondary` (#6B6B6B), Active 시 `--text-link` (#8B7BB8)

### Image Treatment
```css
/* 샵 카드 이미지 */
.shop-image {
  border-radius: 16px;
  aspect-ratio: 16 / 9;     /* 가로:세로 = 16:9 */
  object-fit: cover;
  overflow: hidden;
}

/* 프로필 이미지 */
.profile-image {
  border-radius: 50%;       /* 원형 */
  aspect-ratio: 1 / 1;      /* 정사각형 */
  object-fit: cover;
}
```

---

## 📱 6. 반응형 디자인 (Responsive Design)

### Mobile-First Approach
```css
/* 모바일 기본 (375px - 768px) */
.container {
  padding: 16px;
}

/* 태블릿 (768px - 1024px) */
@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
}

/* 데스크탑 (1024px+) */
@media (min-width: 1024px) {
  .container {
    padding: 32px;
  }
}
```

### Breakpoints
```css
/* Tailwind 기반 브레이크포인트 */
--breakpoint-sm: 640px;
--breakpoint-md: 768px;
--breakpoint-lg: 1024px;
--breakpoint-xl: 1280px;
```

### Touch Target Sizes (모바일 최적화)
```css
/* 버튼 최소 크기: 44px x 44px (iOS Human Interface Guidelines) */
.btn-primary, .btn-secondary {
  min-height: 44px;
  min-width: 44px;
}

/* 네비게이션 아이템 최소 크기 */
.bottom-nav-item {
  min-height: 56px;
  min-width: 56px;
}
```

---

## 🌟 7. 유니크 디자인 패턴 (Unique Design Patterns)

### Pastel Gradient Overlay (헤더 배경)
```css
.header-gradient {
  background: linear-gradient(180deg, 
    rgba(224, 215, 230, 0.3) 0%, 
    rgba(243, 230, 230, 0.1) 50%, 
    rgba(255, 255, 255, 0) 100%
  );
  padding-top: 40px;
  padding-bottom: 24px;
}
```

### Filter Chip with Active State
```css
.filter-chip {
  background: #F9F7FB;
  color: #6B6B6B;
  padding: 8px 16px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 500;
  border: 1px solid transparent;
  transition: all 0.2s ease;
}

.filter-chip.active {
  background: #D0C7E8;
  color: #FFFFFF;
  border-color: #B8A8D6;
  box-shadow: 0 2px 8px rgba(208, 199, 232, 0.4);
}
```

### Shop Card with Distance Badge
```html
<!-- 샵 카드 예시 -->
<div class="card shop-card">
  <img src="shop-image.jpg" class="shop-image" alt="샵 이름">
  <div class="card-content">
    <h3 class="heading-3">샵 이름</h3>
    <div class="card-meta">
      <span class="badge badge-info">1.2km</span>
      <span class="body-small text-secondary">₩50,000~</span>
    </div>
  </div>
</div>
```

### Centered Floating Compare Button (네비게이션 바)
```html
<!-- 네비게이션 바 가운데 버튼 -->
<nav class="bottom-nav">
  <button class="bottom-nav-item">홈</button>
  <button class="bottom-nav-item">지역</button>
  <button class="fab-center">견적비교</button> <!-- 가운데 돌출 -->
  <button class="bottom-nav-item">혜택</button>
  <button class="bottom-nav-item">내정보</button>
</nav>

<style>
.fab-center {
  position: relative;
  top: -20px;
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, #E0D7E6 0%, #F3E6E6 100%);
  border-radius: 50%;
  box-shadow: 0 4px 16px rgba(224, 215, 230, 0.5);
  font-size: 12px;
  font-weight: 600;
  color: #2D2D2D;
}
</style>
```

---

## 🎯 적용 가능한 CSS 코드 요약

### CSS Variables (전역 변수)
```css
:root {
  /* Colors */
  --primary-lavender: #E0D7E6;
  --primary-blush: #F3E6E6;
  --secondary-purple: #D0C7E8;
  --secondary-pink: #F8D7DC;
  
  --bg-primary: #FFFFFF;
  --bg-secondary: #F9F7FB;
  --bg-tertiary: #F3F0F6;
  
  --text-primary: #2D2D2D;
  --text-secondary: #6B6B6B;
  --text-muted: #A0A0A0;
  --text-link: #8B7BB8;
  
  /* Typography */
  --font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif;
  
  /* Spacing */
  --spacing-4: 16px;
  --spacing-6: 24px;
  --spacing-8: 32px;
  
  /* Border Radius */
  --radius-small: 12px;
  --radius-medium: 16px;
  --radius-large: 20px;
  --radius-xl: 24px;
  --radius-full: 50%;
  
  /* Shadows */
  --shadow-soft: 0 2px 12px rgba(0, 0, 0, 0.06);
  --shadow-card: 0 8px 24px rgba(0, 0, 0, 0.1);
  --shadow-button: 0 2px 8px rgba(224, 215, 230, 0.4);
}
```

---

## 📦 Tailwind CSS 커스텀 설정

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          lavender: '#E0D7E6',
          blush: '#F3E6E6',
        },
        secondary: {
          purple: '#D0C7E8',
          pink: '#F8D7DC',
        },
        text: {
          primary: '#2D2D2D',
          secondary: '#6B6B6B',
          muted: '#A0A0A0',
          link: '#8B7BB8',
        },
        bg: {
          primary: '#FFFFFF',
          secondary: '#F9F7FB',
          tertiary: '#F3F0F6',
        },
      },
      fontFamily: {
        sans: ['Pretendard', '-apple-system', 'BlinkMacSystemFont', 'sans-serif'],
      },
      borderRadius: {
        'small': '12px',
        'medium': '16px',
        'large': '20px',
        'xl': '24px',
      },
      boxShadow: {
        'soft': '0 2px 12px rgba(0, 0, 0, 0.06)',
        'card': '0 8px 24px rgba(0, 0, 0, 0.1)',
        'button': '0 2px 8px rgba(224, 215, 230, 0.4)',
      },
    },
  },
}
```

---

## 🚀 다음 단계

### 1️⃣ CSS 파일 생성
- `css/design-system.css` 생성 (전역 CSS 변수)
- `css/components.css` 생성 (재사용 가능한 컴포넌트)

### 2️⃣ HTML 페이지 적용
- `index.html` - 홈 페이지 리디자인
- `login.html` - 로그인 페이지 업데이트
- `customer-dashboard.html` - 고객 대시보드 리뉴얼

### 3️⃣ 컴포넌트 라이브러리 구축
- 버튼 컴포넌트 (primary, secondary, small)
- 카드 컴포넌트 (shop-card, info-card)
- 입력 필드 컴포넌트
- 네비게이션 바 컴포넌트

---

## 🎨 결론

이 디자인 시스템은 **파스텔 라벤더 & 블러쉬 컬러**를 중심으로, **매우 둥근 모서리**와 **부드러운 그림자**를 사용하여 **따뜻하고 친근한 느낌**을 제공합니다.

**BeautyCat.kr에 적용 시**:
- ✅ 브랜드 일관성 강화
- ✅ 사용자 경험 (UX) 대폭 개선
- ✅ 모바일 최적화 (모바일 우선)
- ✅ 접근성 (Accessibility) 준수

---

**다음 작업**: 이 디자인 시스템을 BeautyCat.kr에 적용하시겠어요? 🚀
